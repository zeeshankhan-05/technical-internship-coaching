[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Set in Netlify UI → Site settings → Environment variables:
# PUBLIC_SUPABASE_URL
# PUBLIC_SUPABASE_ANON_KEY

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
